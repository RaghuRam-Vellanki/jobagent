version: "3.9"
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./resume:/app/resume
    env_file:
      - .env
    environment:
      - API_HOST=0.0.0.0
    restart: unless-stopped
